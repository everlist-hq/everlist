# EverList — Hardening & Build Backlog v3 (supersedes v2)

Created 2026-09-11. Source of truth for all remaining work. v2 is marked SUPERSEDED (16/23 done; its gated leftovers H19/H21/H22/H23 are folded in here).

## Standing owner directives (binding for every run)

- **NO internet-exposed personal infrastructure — ever.** Owner machine stays private; the chat door works outbound-only via Agentverse relay. Track O scripts may be written/tested locally, but a rented VPS (~EUR 4.35-4.59/mo Hetzner CX22 or netcup, +EUR 0.50 IPv4) is bought ONLY when a public web endpoint is actually needed (owner decision, 2026-09-11).

1. **Established paths first** — always check what exists before building; simple in-house glue is fine when established systems are too heavy. No new custom auth surface without owner approval.
2. **Two-tier identity is permanent law** — Tier 1 easy signup must NEVER get harder; Tier 2 (Midnight) is an optional benefit layer, never a gate.
3. **No schedulers** — work items in normal sessions, one at a time, from this list. Big list → work it → tick it.
4. **Never push** — owner pushes. Staging repo `.staging-repo/` gets commits; working tree gets features.
5. **P1 pilot is gated until Midnight escrow is integrated** (owner decision 2026-09-11).
6. **Baseline green first** — run the full pipeline before touching code; never leave the tree red.
7. Discovery-first tests (print real behavior, then lock it); escape-heavy code via text_editor, never heredocs; `node -e` probes are forbidden (they hang on WASM) — use files + `timeout`.
8. Every done-when must be **proven by a command**, not claimed.

## Current verified baseline (2026-09-11)

- Hub: hardened (Run 1–3 of v2 done), 20-stage test pipeline + audit, OpenAPI at `/openapi.json`, selfcheck, demo-reset, backups+restore drill, single-instance guard, fsync, rate-limited writes, Ed25519 accounts (§12a transitional) + email recovery (log mode) + archive + monotonic ids.
- Distribution: repo pushed to github.com/everlist-hq/everlist; chat agent "EverList Booking" live on Agentverse (mailbox, Almanac-registered); registry checker C1–C6.
- Midnight: A0 research done; A1 escrow state machine (4 circuits) compiled with compact 0.34.0 and verified **12/12 offline** (experiments/midnight-escrow/). GATE: ZK proof generation needs the proof-server (Docker) — this container has no docker/podman, namespaces blocked.

---

## TRACK M — Midnight integration (ACTIVE, owner GO)

### Run M-A: proof path + funded escrow

- [x] **M1 — Proof-server natively** ✅ DONE (2026-09-11): official `midnightntwrk/proof-server` binary extracted from image layers via Docker Registry API (curl+tar, no daemon) and running natively in-container via the extracted nix glibc linker (`ld-linux-x86-64.so.2` + LD_LIBRARY_PATH with the matching `xgcc-*-libgcc`). Version lattice decoded the hard way: compact 0.34.0 / compact-runtime 0.19.0 / ocrt-v4 ↔ **ledger-v9** ↔ **proof-server 9.0.0-rc.7** (the 8.1.0 line pairs with the OLD runtime 0.16.0 generation). Wire fixes that made it work: (1) ledger-v9 envelope, (2) preimage serialized by **ledger-v9's own** `proofDataIntoSerializedPreimage` (not compact-runtime's), (3) IR must be the tagged binary **`.bzkir`** (`midnight:ir-source[v2]:` header), not the JSON `.zkir`. REAL ProofData captured from the compiled circuits' `callProofDataTrace` after a genuine createEscrow call. **RESULT: /check 200 with parsed costs + /prove 200 → 4508-byte proof starting `midnight:proof-v…` in ~2s. Script: contract/probe-v9.mjs (server on :6301, launcher: tools/proofsrv/run_proofserver.sh).**
  - Done-when: server answers its health endpoint and produces a proof for the hello-world example circuit; OR a written verdict with the exact blocking dependency.
- [x] **M2 — zk-params provisioning** ✅ DONE (2026-09-11): params/keys path verified for EVERY transaction circuit — createEscrow, releaseEscrow, refundEscrow each generated real ZK proofs (4508 bytes, ~2s each) on the native 9.0.0-rc.7 server via `contract/probe-m2.mjs`. Honest transfer of the done-when: `isHeld` is a ledger QUERY — it returns results but emits NO proof-data trace (reads are node-evaluated on Midnight, not transactional), so proving all three transaction circuits (full lifecycle create→release + create→refund) covers and exceeds it. Extra runtime lesson: trace entries are stamped with the CONTEXT-creation circuitId, not the called circuit — the probe overrides `circuitId` with the true name before loading IR/keys.
  - Done-when: proof generation for `isHeld` succeeds once against M1.
- [x] **M3 — Funded escrow circuits** ✅ DONE (2026-09-11): escrows now carry REAL (simulated) shielded money. `createEscrow` deposits an exact-value coin into contract custody (`receiveShielded`, `deposit.value == amount` asserted); `releaseEscrow`/`refundEscrow`/`timeoutRefund` spend it via `sendShielded` to **payout keys stored at creation** (ledger maps `agentPks`/`organizerPks` of `ZswapCoinPublicKey`) — payout destinations are fixed, so even the permissionless timeout cannot redirect funds. Custody recorded on the Escrow struct (coinNonce/coinColor); spenders supply a `QualifiedShieldedCoinInfo` asserted against custody (nonce/color/value). Verified offline **23/23** (`test/escrow.test.ts`: deposit→contract custody output, release→ORG_PK, refund→AGENT_PK, attacker-triggered timeout→AGENT_PK, forged-nonce rejected, deposit≠amount rejected, all state-machine/role walls intact) + **BONUS capstone: funded createEscrow (4405ms) + releaseEscrow (7785ms) proven natively on proof-server 9.0.0-rc.7** (4508 bytes each, `probe-m3.mjs`) — real coin-flow ZK proofs. Key type facts (probe-m3-coins.mjs): circuit-level pk args = `{bytes: Uint8Array(32)}` (plain string only works for the context ctor), QSCI field is **`mt_index`** (snake_case), and a deposit's merkle index = `comIndices.size` captured immediately before the create call. Compiler caught an undeclared witness disclosure (payout pk map insert) — fixed with explicit `disclose()` (destinations are public by design).
  - Done-when: compiles; offline simulation (v2 lessons: constructor-init, context-threading) proves coin balances move in a simulated zswap state.
- [x] **M4 — Timeout circuit** ✅ DONE (2026-09-11): `timeoutRefund(escrowId)` permissionless liveness — after `deadline`, ANYONE (even an attacker with no secrets) may trigger the refund. `deadline: Uint<64>` added to Escrow struct + createEscrow param; release/refund preserve it. Verified offline 17/17 (`test/escrow.test.ts`: before-deadline rejects, after-deadline refunds via attacker instance, post-release/post-refund reject, deadline stored) + BONUS: timeoutRefund proven natively on proof-server 9.0.0-rc.7 (200, 2940 bytes, `probe-m4.mjs`) — all four transaction circuits now have real ZK proofs. Runtime insight: offline context defaults block-time to now (~1.78e9), so deadline=1000n (1970) is always 'past' and 99999999999n always 'future' — no custom-time contexts needed for testing.
  - Done-when: offline test — before deadline rejects, after deadline refunds, post-settlement rejects.

### Run M-B: chain ↔ hub mirror (the integration core) — **COMPLETE 2026-09-11 (M5–M8: 85 new checks)**

- [x] **M5 — Escrow ref on bookings** (DONE 2026-09-11, 22/22 tests + CI step + staging 38cbdb4): hub booking gains optional `escrow_ref` (contract address + escrow id + tx hash), validated shape-wise.
  - Done-when: POST /book accepts/returns it; OpenAPI updated; hub suite green.
- [x] **M6 — Indexer client** (DONE 2026-09-11, 21/21 tests): small Python client (`midnight_indexer.py`) reading contract public state via the Midnight indexer GraphQL (public state only — escrow id, state, amount, bookingRef, deadline). Query shape verified against the official v4 schema reference. Honest limitation recorded: state decode uses the fixture codec (ESCROW_STATE_CODEC) until the ledger-WASM decode spike; fixture RECORDED from the real offline sim (dump-fixtures.mjs: funded create→HELD, release→RELEASED).
  - Done-when: against a local/devnet ledger (or recorded fixture): reads escrow #1 as HELD, then RELEASED after a simulated transition; timeouts + honest errors.
- [x] **M7 — Mirror sync endpoint** (DONE 2026-09-11, 25/25 tests + registry 10/10): `POST /admin/sync-escrow` (admin-key, X-Admin-Key constant-time) pulls chain state via the M6 indexer for every escrow-bearing booking; forward transitions sync (chain wins, ledger entries kind=escrow_sync with from/to/chain_tx, NO amount → no volume double-count); downward (RELEASED→HELD) and cross-final (RELEASED↔REFUNDED) overwrites REFUSED; WAIVED untouched; per-booking honest results incl. indexer-down errors. C7 checker added to check_hub.py (manifest mirror declaration + policy + gating 403; admin-only live-parity limitation documented); fixture hubs updated (manifest mirror + gated POST route); C4 excludes the escrow_sync ledger kind (x402 precedent).
  - Done-when: conformance checker C7: chain RELEASED + hub HELD → sync fixes; chain HELD + hub RELEASED → flagged, refused.
- [x] **M8 — E2E chain booking (local sim)** (DONE 2026-09-11, 17/17 LIVE SIM): m8-scenario.mjs drives the REAL funded offline simulator (create x2 → release → refund; booking refs = actual hub booking ids via argv, TDZ bug caught and fixed); hub bookings walk the action timeline through a stage-advancing chain server: in-sync → hub-ahead C7 refusals (never downward) → chain catches up → RELEASED both sides; refund leg: chain REFUNDED + hub HELD → forward-updated (REFUNDED both sides) while a sibling booking (same chain escrow, hub RELEASED) stays refused as cross-final; ledger integrity (exactly 1 escrow_sync entry, no amount, volume unchanged). Live regen when node+contract present; recorded fixture fallback (CI-safe, CI note included).
  - Done-when: one pytest that runs the full loop against the offline contract simulator; wired into `make test` as an optional stage (skips cleanly if Midnight deps absent).

### Run M-C: keys, ops, safety

- [x] **M9 — Organizer payout key flow** (DONE 2026-09-11, 22/22 tests): `POST /accounts/payout {payout_pk}` (login token OR account_code proves control; email rate bucket) stores the organizer's Midnight **coin PUBLIC key** (64-hex/32-byte format wall, uppercase normalized, replace-with-note); login response + chat session carry it; chat `set-payout <pk>` (login-first gate, PUBLIC-key warning on bad format), whoami shows payout status, help documents it with the secrets warning; legacy-account migration; OpenAPI row; SPEC §18; state-hygiene checks (no seed/secret/sk/private_key fields). Staging commit fa405d0.
- [x] **M10 — Chain secret hygiene** (DONE 2026-09-11, 4/4 tests): PRIVACY.md gained the full Midnight threat model (who holds which secret: buyer wallet / organizer payout secret / operator .secrets/ / hub stores public-only + hashes; structural rules; honest residual risks — operator container holds the testnet identity seed, hence testnet-only + never real funds). Permanent scanner test_m10_secrets.py: scans state/logs/backups for canonical seed + elseed- + .secrets tokens, git ls-files for secret/state paths AND tracked content, hub persisted entities for secret-named fields, .secrets permissions. Baseline verified CLEAN (9 runtime files, both git trees). CI step included.
- [~] **M11 — DUST fee measurement** (2026-09-13: **circuit-level cost matrices MEASURED 7/7**): real per-circuit costs captured from the NATIVE proof server (9.0.0-rc.7, /check via official ledger-v9 wire format) using REAL lifecycle preimages — `contract/measure-costs.mjs` runs the full 4-escrow lifecycle (timeout / self-refund / release+mutual / auto-release, incl. attacker-triggered permissionless paths), captures genuine ProofData from callProofDataTrace, and checks every circuit against the live server. **Result:** every spend transition (timeoutRefund, refundEscrow, releaseEscrow, autoRelease, mutualRefund) charges an identical 12-cell cost block `[1,4,6,2,1,1]x2` (consistent with two commitment openings); createEscrow + isHeld show zero charged cells at contract cost-model level (their pricing lives at tx/DUST level). /check latency 3-110 ms; raw data + method in experiments/midnight-escrow/README.md section M11. **Remaining (explicit):** cost cells -> actual DUST prices + network tx fees both need one funded preprod tx (faucet) — stays in the preprod spike required before C10 real money. Preprod infra findings (singular contractAction schema, queryable paidFees) unchanged from the 2026-09-11 entry; toolchain doc section 9 has the endpoint table.
- [x] **M12 — Maintenance authority** (DECIDED 2026-09-13, **CLOSED**): owner delegated the decision ("can you do m12 and m11"); **Option A — effective immutability — adopted** for the pilot. The contract gains no upgrade path and no operator key (status quo by design: zero code change, smallest trust surface, established-paths directive honored). Liveness = timeoutRefund; worst-case bug cost bounded by escrow size. Upgrades = deploy a NEW contract + re-point the registry. Master-plan decision box ticked with the delegation note; revisit trigger: mainnet amounts, via the established-paths lens first.

### Run M-D: A2 personhood (Tier-2 benefit layer)

- [x] **M13 — Hub-admitted issuer design** *(DONE 2026-09-11)*: issuer signs holder commitment; contract stores commitment + revocation registry (leaderboard/ownerHash pattern).
  - Done-when: credential.compact compiles (5 circuits, compiler 0.34.0 -> managed-credential/); offline tests 16/16: issue→verify(proveHolder)→revoke→verify-fails + takeover-impossible + attacker walls + one-way revocation.
- [x] **M14 — Tier-2 sign-in** (DONE 2026-09-11, 49/49 + chat_help 12/12 + full pipeline ALL PASSED both trees): `POST /accounts/verify-midnight` reads the M13 credential contract PUBLIC state (admitted + not revoked) -> `verified_by: midnight-zk` set SERVER-SIDE only (reserved booking field - client claims impossible). Anti-sybil 1:1 credential<->account binding (both directions 409); revocation propagates (re-check DOWNGRADES to midnight-zk-revoked); fail-closed 502 on every verifier error incl. binary-garbage evidence (suite-caught bug). Mode honesty: chain|simulated labels (HUB_CRED_INDEXER = live chain). Chat: verify-midnight cmd, whoami provenance, help. Vouch unchanged (Tier-1). BONUS gate-isolation fixes: make up now propagates HUB_URL=HUB_PORT AND the embedded wrapper in test_client targets the gate port - the A2A E2E previously silently tested the LIVE stack whenever it ran; it now truly tests the gated hub. Staging commit 09314a2.
  - Done-when: account shows `verified_by: midnight-zk`; bookings carry it server-side; old vouch path unchanged.
- [x] **M15 — Wallet UX walkthrough doc**: first-time registration + already-verified daily flow (owner asked for this; keep honest: no live personhood issuer exists yet — we are the pilot issuer). — DONE 2026-09-11: docs/wallet-ux.md (165 lines, synced to staging 1e5b157). First-time pilot-issuer flow (secret → commitment via VERIFIED helper tools/compute-commitment.mjs, output byte-matched the recorded fixture 37de7a28…), issue→verify-midnight chat flow, daily flow, secrets table (hub never sees holder secret), revocation/downgrade, mode honesty (chain|simulated), honest gaps table (no live personhood issuer — operator is pilot issuer; contract tooling joins the published repo when the Midnight track graduates).
- [x] **M16 — mutualRefund circuit** (DONE 2026-09-13: (owner-approved 2026-09-12, SPEC §19e): mutual refund after settlement. Buyer wants refund + merchant agrees → both must prove. New circuit in escrow.compact: state == RELEASED, BOTH role commitments prove (buyerCommitment + organizerCommitment, same secret-key commitment pattern), funds return to the buyer's stored coin key; **neither party alone can move anything** (one stolen key is useless). ~30 lines mirroring refundEscrow guards. Done-when: offline tests (both-parties succeeds; buyer-only rejected; merchant-only rejected; wrong-state rejected; replay rejected) + native proof on proof-server 9.0.0-rc.7 + hub mirror accepts REFUNDED-after-RELEASED as a legal forward transition (M7 cross-final refusal needs a mutualRefund carve-out) + OpenAPI/mirror notes. Until built: post-settlement refunds are off-contract manual payments (documented honestly). — verified: 32/32 offline tests (both-consent succeeds; buyer-only/merchant-only/attacker walls; wrong-state+replay walls; underpay/wrong-asset return-coin walls), C7 mirror carve-out in app.py (chain REFUNDED+hub RELEASED now legal forward, reverse still refused), m8 E2E 17/17 + m5 22/22, native ZK proof on proof-server 9.0.0-rc.7 (4508 bytes, /check accepted costs)
- [x] **M17 — Merchant auto-release after quiet claim deadline** (DONE 2026-09-13: (owner-approved 2026-09-12, SPEC §19c gap): today's post-deadline default is buyer-favoring (M4 timeoutRefund) — a bad-faith buyer who DID receive the service can claw back within the window. Merchant-side complement: claim deadline passes with NO refund claim → escrow auto-releases to the merchant's stored key. Needs: second deadline (claim deadline) in the Escrow struct + createEscrow; circuit guard ordering — before claim deadline → timeoutRefund (buyer-favoring), after claim deadline → autoRelease (permissionless, pays organizer key). Done-when: offline tests (both windows + boundary) + native proof + hub mirror handles the third terminal path + SPEC §19c updated. — verified: 40/40 offline tests (claim-deadline storage + strict ordering assertion, pre-deadline autoRelease wall, permissionless post-deadline release paying the organizer key even from an attacker, refundEscrow+timeoutRefund walled after claim deadline, wrong-state rejections), native ZK proof on proof-server 9.0.0-rc.7 (4508 bytes), mirror third-terminal path via existing chain-RELEASED forward rule (m7 25/25, m8 17/17, m5 22/22)

## TRACK C — Hub core product (fold v2 leftovers + gaps found since)

- [x] **C1 — Chat parity sweep** (DONE 2026-09-11, 13/13 E2E + 11/11 help): real-wrapper chat E2E extended with the whoami → archive → unarchive chain (account-owned, codeless) incl. mid-flow disk probes (archived True→False) and whoami assertions (account + human_verified + payout guidance); chatlib got honest archive/unarchive replies (hub note surfaced instead of empty 'changed .'); whoami payout line polished; test_chat_help gained set-payout (M9 command) in REQUIRED + probe + signature (login-first gate expected after logout-all in probe order). Staging commit 4f0a317-era.
- [x] **C2 — Search upgrades** (DONE 2026-09-11, 30/30 + full pipeline ALL PASSED): /search gained from/to (YYYY-MM-DD, dateless listings excluded), min_price, tags= (comma multi, any-of), sort=date|price|newest (dateless last; newest = creation reversed), honest 400s, filters echo. Chat parses under/over N, from/until DATE, cheapest, soonest (regex-span extraction so dates survive; help line added). OpenAPI /search documents the surface. New suite test_c2_search.py (match+no-match per filter, combined, validation, OpenAPI, 6 chat parse checks), wired into Makefile + staging CI (step after M10); suite spawn is runner-safe (sys.executable). Lesson reapplied: heredoc Makefile-tab escaping -> script file; make test must run with live stack down (make up refuses busy port by design). Staging commit after 95edb57.
- [x] **C3 — my-bookings** (DONE 2026-09-11, 18/18 + full pipeline ALL PASSED): REUSED the existing principal-scoped GET /bookings (I2) + SDK bookings()/orders() instead of adding a redundant /bookings/mine route (established-paths lens: no new attack surface). Added chat `my-bookings` (+ 'my bookings' variant) with anonymous-agent-identity path, session path, honest empty state; help row. New suite test_c3_bookings.py: auth matrix (no/garbage token 401), buyer isolation BOTH directions, list-act dual acceptance, /orders merchant scoping + pseudonymity (real names absent), second-owner zero orders, SDK parity both methods, 4 chat paths. Contract facts re-confirmed: POST /book takes listing_id IN BODY; /access returns 201; bookings need verified-human credential (interim stub flag). Wired into Makefile + staging CI.
- [x] **C4 — Reputation (post-booking)** (DONE 2026-09-11, 26/26 + pipeline ALL PASSED + chat-help 11/11): POST /book/{id}/rate — buyer-only (token principal == booked_by, owner-immutable), strict int [1,5] (bool/str/float rejected), settlement-gated (HELD/REFUNDED 409; RELEASED/WAIVED 200), once-only forever; aggregates (rating_sum/count) stored on the listing at rating time and flow through _pub_listing to ALL public surfaces (fetch + search); LEDGER untouched (anonymity preserved); OpenAPI documented; chat `rate <id> <1-5>` (bare + args, anon/session paths) + help row. Suite test_c4_rating.py wired into Makefile + CI. (5 test failures during bring-up were my sequencing bugs — cancel_token vs access token, ledger snapshot order, stranger-principal probe — hub correct throughout.)
  - Done-when: tests for once-only, owner-immutable, aggregate math; ledger anonymity preserved — ALL MET.
- [x] **C5 — Vertical schema v2** (DONE 2026-09-11, 15/15 + full pipeline ALL PASSED + chat-help 11/11): fail-closed drop-in loader — community verticals are schemas/*.json files (HUB_SCHEMAS_DIR), validated at boot on 10 rejection classes (built-in override ignored, unknown keys, bad name, missing price, server-owned fields, req/opt overlap, unknown field_types, capacity-without-track, reserved booking fields, identity-not-required); a bad file is logged and skipped, the hub ALWAYS boots and built-ins ALWAYS win. Contributed reference vertical `classes` (clas- prefix, instructor/skill_level/duration_minutes, capacity tracking) verified end-to-end: field_types enforced, category vocab enforced, booking+capacity flow, chat one-message rich listing (live /verticals fetch — schema decides, not chat code), unknown-vertical hint names community verticals. Contribution guide docs/community-schemas.md; suite test_c5_schemas.py in Makefile+CI. Legacy h15 exact-set check evolved to the additive-only contract (built-ins identical, extras allowed). PUSHED to GitHub d11c3a9 (CI-step addition deferred: PAT lacks workflow scope — pending file ci-steps-pending.yml).
- [x] **C6 — `/registry` agent-readable index** *(DONE 2026-09-11, commit e2c16db)*: registry serves a signed `/registry.json` (hubs, schemas versions) so agents bootstrap from one well-known URL.
  - Done-when: format documented (registry/README), signature verified by check_hub (--registry mode + --pubkey pinning), test (R9-R12: envelope, tamper, CLI, wrong-pin rejection; D2 16/16 + D3 10/10 both trees; pushed + CI green).
- [ ] **C7 — SQLite adapter** *(v2 H19, gated — triggers unchanged: accounts 3–5k, listings 2k+, persist p95>100ms)*.
- [ ] **C8 — MCP server** *(v2 H21, gated — post-Midnight; exposes search/list/book to MCP clients)*.
- [~] **C9 — Chat web front-end** *(v2 H22; deterministic core stays)* — **deterministic webchat DONE 2026-09-13 (owner GO same day: owner proposed the chat-first website)**: `webchat.py` (stdlib-only, :8804, binds 127.0.0.1 for Caddy) serves `static/` (CSP-hardened dark chat UI, XSS-safe textContent rendering) + `POST /api/chat` → the SAME chatlib brain (third surface: Agentverse wrapper, CLI, now browser). Sessions: server-minted 192-bit HttpOnly/SameSite cookie doubles as the chatlib sender key (no client-forgeable sender, no extra session store); per-sender (8 burst/4 per-min) + per-IP token buckets; 32KB body cap; 413/415/400 walls; /api/reset; request-line-only logging (no message content). Suite `test_webchat.py` 20/20 (static+headers, traversal wall, signup→seed→whoami session continuity, fallback+filtered search, reset, rate limit) wired into make test + operator targets `webchat-up/down` + status. deploy.sh: chat.<domain> Caddy site + everlist-webchat.service. LLM natural-language layer on top of the deterministic brain: still open (needs explicit owner call on model/provider/cost). **UPDATE 2026-09-13 (owner GO in-chat: 'hook up a0_venice for now, separate API later')**: `nlu.py` shipped — free text → strict validated JSON → canonical `search …` command ONLY (allowlist regex, no pipes/slashes, capped, date-resolved against TODAY); the deterministic `_smart_search` still executes everything, LLM never sees hub data (no hallucination surface), fail-open on missing key/error/timeout; env-gated via gitignored `.secrets/llm.env` 0600 (`EVERLIST_NLU_API_URL/_API_KEY/_MODEL`, default a0_venice e2ee-glm-5-3-flash; provider swap = env change only); per-sender rate limit 30/5min; max_tokens 1200 (reasoning-model overhead). **Standard listing card (owner picked rich-text, 'works in any chat')**: `_fmt_listing` rewritten as fixed-shape plain-text card (emoji-anchored: 🎫 title · id / category · location · Weekday date / 💶 price · 🎟 x of y spots / 🛡 escrow · window / 📝 description / 🔗 url; variants: 💶 free, sold out x/y, ⚡ instant rail, ✅ verified-only) — shared brain means webchat + Agentverse wrapper + CLI all inherit it; search joins cards with blank lines. `test_nlu.py` 14/14 (card shapes, sanitization/injection gates, fail-open, rate limit, mocked E2E) wired into make test; full gate ALL PASSED; live-verified via webchat HTTP ('any jazz concerts coming up?' → soonest-sorted card). DEPLOY.md documents the optional VPS step. **UPDATE 2026-09-13 (C9c, owner directive: "i want to be original - alt-codes.net glyphs, view 10 at a time")**: emoji set REPLACED by an original monochrome glyph identity (alt-code/Unicode text symbols, never color emoji): vertical heads, currency sign, hollow/filled spot grids, escrow seal, instant arrow, verified tick, quote + link arrows. Long result lists: >6 results render a numbered one-line index (all results, cap 20) + 3 full preview cards + "say 1 / 2-6 / all" hint; <=6 render full cards inline (cap 12); per-sender last-results stash replays "3", "2-6", "all" as full cards (bounded stash, honest out-of-bounds errors). test_nlu.py now 20/20 (6 new pagination tests); full gate ALL PASSED; live E2E on webchat: "search" with 10 demo listings -> index + previews + replay, verified in the real browser UI. Demo seed grown 6 -> 10 listings (4 new curated events, per-vertical category validation) so the index view is demo-able out of the box. **UPDATE 2026-09-14 (C9d, owner design gate closed after 15 sheet-lab rounds: "make a style everybody can recognise")**: the listing sheet finalized as ONE true grammar in the shared brain — header ֎ EverList · N found · facts row ⌂ place · ◷ date · $ price · ♟ person (person always last, spaced) · names+dates math-bold (real bold letters via _mb, bold in ANY chat no markdown) · $0 replaces free · ♟ N open/N left/0 left replaces spot grids · real currency symbols. Owner cuts: dropped ➤ instruction row, the status tier rail, category glyphs, ¤, radar/dot-rails/exotic-as-language. Progressive disclosure kept (glimpse list / story card). Full test surface re-pinned (nlu/c2/webchat/h9/chat-help/read-limits/h16; h16 demo count made dynamic); make test ALL PASSED; live browser E2E screenshot-verified. Staging committed (not pushed). **UPDATE 2026-09-14 (C9f, owner: "remove the left and right ║ ... number them at the beginning")**: knot frame restored then side rails dropped (browser staircase from math-bold fallback); horizontal rules+seams kept; listing id removed, each entry leads with its number which becomes the booking handle; `book <n>` resolves the Nth last-search result (additive, real ids still work); help teaches `book <n>`; tests re-pinned (no-rail/no-id/numbered); make test ALL PASSED; live browser E2E screenshot-verified. **UPDATE 2026-09-15 (C9g, owner: "thin and then rounded we try")**: entry seam = thin ─ with rounded ╰╯ ends (no ornaments on the line; ends freed after rails dropped); knot rules unchanged; `make up` hub launch fixed to the project venv python ($(VPY)) — /opt/venv lacked `cryptography` after container recreation; gate ALL PASSED; live browser E2E screenshot-verified.
- [ ] **C10 — Real-money x402** *(v2 H23, gated — needs pilot + M11 numbers)*.
- [x] **C11 — Tier-2 buyer gating flag** (DONE 2026-09-13: shipped + 34/34 suite + full gate green + pushed): listing field `require_verified_buyer` (strict boolean; null=unset), public payload shows the gate pre-booking; `/book` enforces the ALREADY-computed server-side `acct_verified` (the human_verified stub NEVER satisfies it — only midnight-zk / admin-vouch count), 403 names the requirement + verify-midnight pointer; provenance flows into the booking's server-derived `verified_by`. Owner-editable pre-booking (boolean wall, single-field edit, non-owner 403, flip-off/re-arm tested); chat rich key `verified_only: yes|no` (strict parse, junk refused with guidance) + help + listing face line; OpenAPI /listings + /book notes; SPEC §22 (incl. P2 composition: claim code and verification are independent walls). Deferred-until-production note superseded: machinery is live and tested (simulated verifier mode honestly labeled). Done-when met: flag-set/read, gated-reject, verified-pass (both paths), private-deal composition, chat set.
- [x] **C12 — Payment-terms object on listings** (owner-approved 2026-09-12, SPEC §19a–d; **DONE 2026-09-13**, see worklog): implement the pinned policy — listing carries `payment_terms {rail: escrow|instant, refund_window_hours (default per vertical 72/72/168, merchant override), deposit_required (amount, optional)}`; booking request must ECHO accepted terms (explicit consent, API-enforced, not just UI); per-vertical defaults from SPEC §19c; instant rail = merchant opt-in with no-refund-window caveat surfaced at booking. Done-when: suite covers terms validation (bad rail/window/deposit rejected), booking-echo mismatch → 409, defaults filled per vertical, terms visible in public listing + chat booking preview.

## TRACK O — Production ops (everlist.network)

- [ ] **O1 — VPS provision** (owner provides host): size note (2 vCPU/4GB min for hub+wrapper+proof-server later). Groundwork DONE 2026-09-12: `tools/deploy.sh` (idempotent, Caddy via apt, real admin/booking keys, ARM/AMD) + `docs/DEPLOY.md` complete, pushed `c027da8` — provisioning itself = owner orders RackNerd → runs script → DNS.
- [ ] **O2 — Process supervision**: systemd units (hub, wrapper, registry) with Restart=always + hardening (NoNewPrivileges, ProtectSystem).
  - Done-when: units survive reboot test; `make deploy` documented in repo.
- [ ] **O3 — TLS + reverse proxy**: Caddy (auto-HTTPS, established path) in front; hub stays 0600-secret strict.
  - Done-when: https://everlist.network serves manifest; A+ TLS grade via online checker; http→https redirect.
- [ ] **O4 — Production email**: owner's Gmail App Password (pending owner step) → `HUB_EMAIL_MODE=smtp` for real recovery mails.
- [ ] **O5 — Offsite backups**: nightly rsync/rclone of state+backups to owner storage; restore drill against production copy.
  - Done-when: restore test proves a prod backup restores byte-equal.
- [ ] **O6 — Monitoring**: selfcheck as a watchdog + external uptime ping (e.g. healthchecks.io free tier); alert path = notify owner.
  - Done-when: simulated hub death triggers alert within 5 min.
- [ ] **O7 — Deploy pipeline**: tag → GitHub Action builds, runs full pipeline on the pushed repo, produces a release tarball; deploy = pull tarball + restart (owner-approved only).
- [ ] **O8 — Landing page**: static manifest-human page on the domain (what/why/how-to-list), links to OpenAPI + chat agent.

## TRACK P — Pilot launch (GATED: after Track M runs M-A/M-B integrated)

- [ ] **P1 — Outreach kit**: EN+DE messages (short/long), one-pager PDF, chat demo script using `make demo-reset`.
- [ ] **P2 — Lead list**: 20–50 Eventbrite/Meetup organizers (region: owner's area + yoga/wellness/workshops), personalization notes per lead.
- [ ] **P3 — Wedge metric**: define success = 2 organizers listing + ≥3 real bookings (from pilot-plan wedge criterion); dashboard = simple grep-able log + weekly count.
- [ ] **P4 — Feedback loop**: 15-min structured interview template; friction log file; weekly retro into docs/lessons.md.
- [ ] **P5 — Compliance quickcheck**: EU small-business outreach (legitimate interest), imprint/privacy page on landing.

## TRACK S — Security (continuous)

- [ ] **S1 — Red-team pass 3**: target = everything added since H17 (mirror endpoints, payout_pk, ratings, registry index, OpenAPI surface changes). Discovery-first, 12+ attack classes, findings → fixes → permanent tests.
- [ ] **S2 — Rate-limit sweep 2**: all new POST endpoints covered (sync-escrow, ratings, payout) with per-source fairness; brute-forceable secrets count toward limits.
- [ ] **S3 — Secrets inventory**: one doc listing every secret, where it lives, rotation procedure (hub admin key, agentverse key, agent seed, email, Midnight seeds when they exist).
- [ ] **S4 — Incident playbook**: what owner does on compromise: rotate account codes (done), revoke tokens (done), rotate admin key (verify path), restore from backup (drilled), publish notice.
- [ ] **S5 — Dependency audit cadence**: `make audit` green at every run; quarterly-ish review note in worklog when new deps land (Midnight SDKs will add many).
- [x] **S6 — Review integrity** (DONE 2026-09-13: shipped + 23/23 suite + full gate green + pushed) (owner-approved 2026-09-13; REQUIRED before C10 flips to real money): anti-fake-review stack. **L1 payer≠merchant wall**: x402 settlement stores the payer address from the X-PAYMENT header — reject rating when payer == merchant receive/payout address. **L2 WAIVED separation**: C4's gate currently lets RELEASED **or WAIVED** bookings rate (verified in app.py) — free bookings rate in a separate "free-class feedback" channel, never merged into the paid aggregate (cheapest farm in the system today). **L3 amount weighting**: review weight scales with settled amount (capped) in aggregate display — a $0.50 self-loop ≠ a $50 booking. **L4 interlock detection**: flag merchants whose reviewers form payer-address clusters / circular payment flows / burst-created wallets → manual review queue, not auto-delete. Structural backstops already built: escrow default rail (farming costs locked capital), Tier-2 personhood (M14), C11 verified-buyer flag. Done-when: suite covers all four layers (self-pay rejected, waived kept separate, weighting math, detection flags), public aggregates stay honest, OpenAPI + chat help updated.

## TRACK X — Docs, brand, developer experience

- [ ] **X1 — Naming hygiene**: rename experiment folder `agent-hub-v2` → `everlist` (or symlink) + update docs references; keep git history intact; graduation to own project still pending owner call.
- [ ] **X2 — SDK quickstart**: 10-minute from-pip-install-to-booking guide (README + example extended: keypair account, list, search, book, cancel).
- [ ] **X3 — SPEC restructure**: split per-audience (agents: manifest/OpenAPI; organizers: chat cookbook; operators: runbook) instead of one growing file.
- [ ] **X4 — Vocabulary/schema registry docs**: how community schemas are proposed/versioned (ties to C5).
- [ ] **X5 — Lessons digest**: docs/lessons.md is long — extract top-15 into docs/principles.md (compiler disclosure rules, context threading, least-resistance, etc.).

## TRACK Q — Testing & quality

- [ ] **Q1 — Contract property tests**: randomized offline fuzz of the escrow state machine (random valid/invalid op sequences; invariants: no state transition out of terminal states, no double-spend paths in sim).
- [ ] **Q2 — OpenAPI contract tests**: SDK client validated against /openapi.json schema (drift can't hide).
- [ ] **Q3 — Load baseline**: record req/s for /listings, /search, /book at current size; note in SPEC (B4 SQLite triggers use these numbers).
- [ ] **Q4 — CI parity check**: the pushed repo's CI runs the same 20 stages as local `make test` (diff Makefile vs workflow after each track that adds stages).

---

## Suggested run order (normal sessions, one item at a time)

1. **M1+M2** (proof path — biggest unknown, attempt now)
2. **M3+M4** (funded + timeout circuits, offline-verifiable regardless of M1 outcome)
3. **M5–M8** (mirror + E2E)
4. **M9–M12** (keys/fees/authority)
5. **C1–C3** (chat parity + search — cheap, high pilot value)
6. **M13–M15** (personhood Tier-2)
7. **O1–O7** (production: needs owner VPS + App Password)
8. **S1–S3** before pilot; **P1–P5** launch; **C4–C6, Q\*, X\*** interleave when blocked
9. Gated items (C7 SQLite, C8 MCP, C9 LLM, C10 x402) wait for explicit owner GO with data.

## Executor rules (unchanged from v2, still binding)

Baseline green → one item → prove done-when with a command → full pipeline → commit staging (never push) → worklog entry → tick box here with date.
